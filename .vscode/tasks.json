{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/src/strategy/strategy.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
        //TODO: Add tasks for observer pattern
    ]
}